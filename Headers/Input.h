#ifndef INPUT_H
#define INPUT_H
float Calculate (char *s, int m);

float inputlnt (float min,float max);

char *inputString(char *buffer, unsigned int bufferLen);

char* enterspring (char *buf , int c);
#endif // INPUT_H
